<% @favorites.each do |photo| %>
  <a class="fancybox" rel="group" href="<%= photo_path(photo) %>"><img class="worldmap_photos" src="<%= photo.image_url %>/convert?w=300&h=300&fit=crop"></img></a>
<% end %>

